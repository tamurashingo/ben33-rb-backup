<%= form_for([@event, @event.attends.build]) do |f| %>
  <p>
    <%= f.label :attend %>
    <%= f.select :attend_type, [["出席", "attend"], ["欠席", "cancel"]] %>
  </p>
  <p>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
